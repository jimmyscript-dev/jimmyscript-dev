const movie = {
    title: 'Dune',
    year: '2021',
    length: 155,
    book: true
};

const {title, year, length, book} = movie;
console.log(title);
console.log(year);
console.log(length);
console.log(book);

